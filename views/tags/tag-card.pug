//- Mixin cho Tag Card
mixin tagCard(tag)
  .card.shadow-sm.mb-4
    .card-body
      h5.card-title= tag.name
      .card-text.d-flex.gap-1
        a.btn.btn-sm.btn-primary(href="/tags/edit/" + tag._id) Edit
        a.btn.btn-sm.btn-danger(href="/tags/confirm-delete/" + tag._id) Delete
