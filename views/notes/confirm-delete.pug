extends ../shared/layout

block content
  .container
    h1 Confirm Delete
    p Are you sure you want to delete the note titled "#{note.title}"?

    // Form to confirm deletion
    .d-flex.gap-1
      a.btn.btn-danger(href=`/notes/delete/${note._id}`) Delete
      a.btn.btn-secondary(href="/notes") Cancel
